{
  "name": "postory-extension",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "webpack": "^4.41.0",
    "webpack-cli": "^3.3.9"
  },
  "scripts": {
    "build": "webpack --config webpack.config.js && cp manifest.json dist && cp popup.html dist",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {
    "sweetalert": "^2.1.2"
  }
}
