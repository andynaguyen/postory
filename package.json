{
  "name": "postory-extension",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "webpack": "^4.41.0",
    "webpack-cli": "^3.3.9"
  },
  "scripts": {
    "build": "webpack --config webpack.config.js --mode=development && cp manifest.json dist",
    "build:prod": "webpack --config webpack.config.js --mode=production && cp manifest.json dist",
    "clean": "rm -rf ./dist"
  }
}
